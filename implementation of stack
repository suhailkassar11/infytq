#implementation of stack:
stack = ["Amar", "Akbar", "Anthony"]
stack.append("Ram")
stack.append("Iqbal")
print(stack)
#removes the last items
print(stack.pop())
print(stack)
# Removes the last item
print(stack.pop())
print(stack)
